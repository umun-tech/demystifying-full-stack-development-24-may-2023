import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'in.umun.sampleApp',
    appName: 'Sample App',
    webDir: "../../dist/apps/sample-app",
    bundledWebRuntime: false
}

export default config;
